const PostTemplate = ({children}) => {
    return(
        <diV className="flex justify-center max-w-screen-xl m-auto flex-wrap">
            {children}
        </diV>
    )
}

export default PostTemplate;